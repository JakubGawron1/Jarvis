# OS tools

When the user asks to open an application, write a note, or add a calendar event, use the available tools instead of pretending.

Notes go to `vault/` as Markdown. Calendar events go to `vault/calendar.json` and can be exported as `.ics`.
