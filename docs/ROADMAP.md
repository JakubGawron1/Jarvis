# Roadmap

0. Monorepo skeleton, protocol, persona, vault.
1. `jarvisd` + CLI, LLM router, SQLite, Windows/Linux tools, confirm.
2. Voice: Whisper PL+EN, Piper PL, Kokoro/Piper EN, wake word, PTT.
3. Next.js HUD (Arc Reactor + text).
4. Flutter Win/Android/Linux + FFI + pull_core for `.so`.
5. Mesh: device_hello, live I/O vs leader handoff, Tailscale, Turso.
5b. Render on-demand core.
6. Self-mod on desktop, publish artifacts, pull_core everywhere.
7. Jarvis Linux: Arch + Plasma; HUD overlay (Super+J); one rootfs → QEMU, VirtualBox, ISO/USB.

Later: MQTT/IoT, screen OCR, JarvisOS kernel (`os/`).
